// shard-file for main.cpp

void ap::intro() {
	//start = new Start();
	//add(start);
	
	/*Text *wit = new Text(monospace, &WHITE, "Walking the ship like waddap");
	wit->sscale(3.D);
	wit->sy(en::HEIGHT-monospace.gh()*3);
	//wit->sa(.2);
	wit->position();
	//wit->sx( (en::WIDTH/2) - ((wit->gtextw()*3)/2) );
	int s = monospace.getglyph(' ')->w*3;
	wit->sx( (en::WIDTH) - (wit->gtextw()*3) - s );
	wit->position();
	
	add(wit);*/
}

/*bg = new Draws();
	bg->sprite(768, 0, 256, 256);
	bg->dim(0, 0, en::WIDTH, en::HEIGHT);
	bg->texture = true;
	draws.push_back(bg);*/
	
	/*galaxy = new Draws(
			const_cast<en::Region *>(&regions::galaxy),
			(&textures::galaxy)
	);
	
	galaxy->dim(0, 0, en::WIDTH, en::HEIGHT);
	galaxy->sfit(false);*/
	
	/*en::Draws *popup = new en::Draws();
	popup->sclr(&DARKGRAY);
	popup->sa(.5);*/
	
	// version overlay label
	/*overlay = new Text(&normal, &WHITE, "alpha");
	overlay->dim(normal.getglyph(' ')->w/2, 0, 0, 0);
	overlay->sa(.2);
	overlay->position();*/
	//draws.push_back(overlay);
	
	/*int y = 200;
	Font *f = &normal;
	Text *vgay = new Text(f, &TEXTOFF, txt::MAELSTROM);
	vgay->position( );
	vgay->dim((en::WIDTH/2)-(vgay->gw()/2), (en::HEIGHT/2)-f->gb()+y, 0, 0);
	vgay->position();*/
	
	/*Text *titel = new Text(&monospace, &TEXTOFF, txt::MESSAGE);
	titel->position();
	titel->dim((en::WIDTH/2)-vgay->gw()+titel->gw(), (en::HEIGHT/2)-f->gb()+y, 0, 0);
	titel->position();*/
	
	//popup->dim((en::WIDTH/2)-(vgay->gw()/2)-16, (en::HEIGHT/2)-f->gb()-16+y, vgay->gw()+32, f->gh() + 32);
	//popup->dim(50, 50, 200, 200);
	
	//draws.push_back(galaxy);
	//draws.push_back(popup);
	//draws.push_back(titel);
	//sprites.push_back(ply);