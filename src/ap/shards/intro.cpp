#include "stdafx.h"

// shard-file for main.cpp

void ap::intro() {
	//start = new Start();
	//add(start);
	
	/*Text *wit = new Text(monospace, &WHITE, "Walking the ship like waddap");
	wit->sscale(3.D);
	wit->sy(en::HEIGHT-monospace.gh()*3);
	//wit->sa(.2);
	wit->position();
	//wit->sx( (en::WIDTH/2) - ((wit->gtextw()*3)/2) );
	int s = monospace.getglyph(' ')->w*3;
	wit->sx( (en::WIDTH) - (wit->gtextw()*3) - s );
	wit->position();
	
	add(wit);*/
}
